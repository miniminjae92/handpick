{
  "name": "handpick",
  "version": "0.3.0",
  "private": true,
  "description": "Select content from supported webpages and copy or save it as clean Markdown.",
  "scripts": {
    "test": "node --test",
    "check": "npm run check:syntax && npm test && npm run check:drift",
    "check:syntax": "node --check extension/background.js && node --check extension/content-script.js && node --check extension/report.js && node --check extension/options.js && node --check extension/popup.js && node --check extension/welcome.js && node --check shared/converter-core.js",
    "check:drift": "./scripts/check-sync-drift.sh"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
